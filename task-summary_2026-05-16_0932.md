# Task Summary: 安装 Claude Code 并配置 DeepSeek 模型

## Objective
安装 Claude Code CLI，并配置 DeepSeek V4 Pro [1M] 作为其模型。

## Key Findings
- ✅ Claude Code 已安装成功：v2.1.143 (via `npm install -g @anthropic-ai/claude-code`)
- ⚠️ Claude Code 原生不支持 DeepSeek / OpenAI 兼容 API
- 仅支持：Anthropic API, Amazon Bedrock, Google Vertex AI, Microsoft Foundry
- `--model` 参数只接受 Anthropic 模型名（sonnet/opus 等）
- 需要通过 API 代理/网关将 Anthropic API 格式转换为 DeepSeek 格式，配合 `ANTHROPIC_BASE_URL` 使用

## Next Steps
- 等待用户确认：是否有 DeepSeek API Key，是否有 API 代理服务
- 根据用户情况配置代理方案
