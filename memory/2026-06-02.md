# 2026-06-02

## 锻件计算器 - 云同步按钮可见性修复

- 用户反馈「初始化云同步」按钮看不到
- 原因：`btnInitSync` 和 `btnRecoverGist` 之前默认 `style="display:none"`，只在特定 localStorage 条件下才显示，新用户打开面板时看不到
- 修复：移除两个按钮的初始 `display:none`，让它们**始终可见**
- 安全保护：两个函数内部已有 Token 检查（未填 Token 会提示），不会裸运行
- 已部署：本地文件 + Vercel + GitHub Pages (docs/index.html) 三处同步
