### 12. 历史记录表格全面重排 + 导出修复

**表格列宽重排（CSS类化，移除内联样式）**：
| 列 | CSS类 | 规则 |
|----|-------|------|
| 日期 | `.rec-date` | max-width:95px, word-break (2行) |
| 客户 | `.rec-cust` | max-width:90px, nowrap+ellipsis (1行) |
| 名称 | `.rec-name` | max-width:110px (2行) |
| 材质 | `.rec-mat` | nowrap (1行) |
| 型号 | `.rec-model` | max-width:120px (3行) |
| 图号 | `.rec-dwg` | max-width:100px (2行) |
| 尺寸概要 | `.rec-summary` | text-align:center, 260-480px |
| 操作 | `.rec-actions` | white-space:nowrap |
| 列间隙 | td padding | 6px 10px (≈5mm间隙) |

**导出修复**：
- `fireLoss` getter: `String()` 包裹防御旧记录中的数字值
- `actFinWt` getter: 添加 `r.actualWeight` fallback 兼容旧记录
- "实际下料(KG)" 列：改用 `actualFinalWeight` 替代带"KG"后缀的 `actualWeight`

### 13. 计算结果汇总表格全面优化（9项）
**列宽重分配**：
- Label: 140→150px（"实际总重（不含火耗）"不换行）
- Formula: 移除nowrap+ellipsis，允许换行（公式完整显示）
- Value: 100→120px（大数值如4553.4 KG从容显示）
- Note: 140→90px + ellipsis（回收50px给关键列）

**视觉优化**：
- 行padding: 13→12px（更紧凑）
- 行hover: #f8fafc→#f0f4f8（更可感知）
- Label颜色: #334155→#1e293b（更深）
- 差额徽章: 18→20px + box-shadow（更醒目）
- Final行: 额外padding 14px（突出最终结果）
- Body padding: 4→2px（减少冗余空白）