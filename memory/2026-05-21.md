# 2026-05-21 工作记录

## 锻件算重报价系统 - 跨设备同步

### 背景
用户有一个锻件算重报价系统（单 HTML 文件），原本只用 localStorage，只能在单台电脑使用。

### 已完成
1. **创建本地服务器** `forging-server.js`（端口 8080），支持同 WiFi 下多设备同步
   - 路径：`C:\Users\Administrator\.qclaw\workspace\forging-server.js`
   - 提供 `/api/ping`、`/api/records` GET/POST 接口
   - 数据存储：`forging-records.json`
   - 前端 HTML 已添加服务器模式检测，自动降级到 localStorage

2. **修复 HTML 路径问题**：服务器最初用 `__dirname` 找 HTML，路径不对，改为使用硬编码工作区路径

3. **识别核心需求**：用户手机在外地、电脑关机时也需要访问和同步数据 → 本地服务器方案不够用

### 推进中
- **云端部署方案**（Vercel 免费平台）：
  - 创建了 `api/records.js` Vercel Serverless Function
  - 创建了带三模式（本地/服务器/云端）的 HTML 版本
  - 支持配置云端 API 地址和同步码
  - 备选方案：jsonblob.com（无需配置即可使用的简易云端）
  - **用户尚未确认是否要部署到 Vercel**，等待用户回复

### 关键文件
- `C:\Users\Administrator\.qclaw\workspace\forging-calculator-template.html` — 主 HTML（已加本地+服务器模式）
- `C:\Users\Administrator\.qclaw\workspace\forging-server.js` — 本地 Node 服务器
- `C:\Users\Administrator\.qclaw\workspace\forging-records.json` — 数据文件
- 临时文件在 `C:\Users\Administrator\AppData\Local\Temp\_` 前缀：
  - `_api_records.js` — Vercel API 函数
  - `_forging_cloud.html` — 云端版 HTML（三模式）
  - `_vercel_package.json` — Vercel 部署包配置

### 待决
- 用户是否选择 Vercel 部署（需要 GitHub 账号）
- 是否需要其他云端方案（如 jsonblob 简易模式，无需注册）
